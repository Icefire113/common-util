[package]
name = "common-util"
version = "0.1.1"
edition = "2024"

[dependencies]
thiserror = { version = "2.0.20", optional = true }

[features]
default = ["formatting", "bin_read"]
formatting = []
bin_read = ["dep:thiserror"]
